import React, { memo } from "react";

export interface TableProps {}

const TableComponent: React.FC<TableProps> = () => {
    return <div>/* TODO: Add JSX */</div>;
};

export const Table = memo(TableComponent);
